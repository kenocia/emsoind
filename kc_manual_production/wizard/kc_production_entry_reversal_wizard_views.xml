<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================ -->
    <!-- Vista del Asistente de Reversión de RP                       -->
    <!-- ============================================================ -->
    <record id="kc_production_entry_reversal_wizard_view_form" model="ir.ui.view">
        <field name="name">kc.production.entry.reversal.wizard.form</field>
        <field name="model">kc.production.entry.reversal.wizard</field>
        <field name="arch" type="xml">
            <form string="Revertir Registro de Producción">
                <div class="alert alert-warning" role="alert">
                    Esta acción creará un nuevo Registro de Producción que revierte
                    el movimiento de inventario del documento original. El registro
                    original NO se modifica (queda como evidencia).
                </div>
                <group>
                    <field name="entry_id" readonly="1"/>
                    <field name="reason" placeholder="Indique el motivo de la reversión..."/>
                </group>
                <footer>
                    <button name="action_confirm_reversal" string="Confirmar Reversión"
                            type="object" class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>
